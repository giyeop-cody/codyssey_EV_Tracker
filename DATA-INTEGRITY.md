# 데이터 정합성 사고 기록 — "사이트엔 있는데 대시보드에 없다"

codyssey UI에는 보이는 평가가 트래커에 "없어 보인다"는 계열의 신고가 반복됐다.
재발 때마다 같은 조사를 다시 하지 않도록, 검증된 사실 기준으로 원인을 분류하고
대응 절차와 해결책을 이 문서에 누적한다.

---

## 1. 사고 연표 (실측 검증분만)

| 일시 | 증상 | 조사 결론 | 근본 원인 | 조치 |
|---|---|---|---|---|
| 2026-07-18 | 수집 3시간 공백 | `*/30` cron 미발화 관측, 실측 배달 간격 평균 82~110분 | GitHub 스케줄러 지연 | 4스트림 크론 도입 (30분 슬롯당 발화 기회 4회, EV/Jail 공통) |
| 2026-07-18 | 취소 건 노출 방식 혼선 | 표시 정책 문제 | 표시 계층 | CANCELLED 로드 시 화면 제외(`docs/app.js`), JSON에는 보존 |
| 2026-07-18 22:13 UTC | 데이터 커밋 실패 | 동시 run이 같은 생성 JSON을 push해 rebase 충돌 | 동시 실행 충돌 | 커밋 단계 rebase 3회 재시도 + 이번 run 생성본 채택 |
| 2026-07-20 오전 | "평가가 일어났는데 없다" (evlNo 619041·619036·634070·634071) | 전부 JSON에 포함 확인 | 확인 시점 문제: 당일분은 수집 전이라 오전 시점엔 7/20 0건 | 데이터가 있음을 확인, 사용자 재확인 경로 안내 |
| 2026-07-20 밤 | 진행 3건(17:39·17:47 요청단계, 21:30 세션)이 "없다" | 전부 수집됨. 첫 진입: evlNo 619021 행 7/17 12:26, s625015 행 7/20 19:30, 슬롯 636065/636066 7/20 21:38 (KST) | 복합 — 아래 2·3·4·5번 원인 | 캘린더 셀에 진행/요청 카운트 추가 (이번 배포) |
| 2026-07-22 | [Jail] "입실 중인데 현재 재실에 안 뜬다" | 소스(`rest/secom/detail`)에 해당 멤버 기록이 7/19 퇴실 이후 없음. 수집기 원본 아티팩트로 확정 (같은 run에서 타 멤버 19명의 당일 기록 정상 수집) | **원인 6 — 소스 시스템에 이벤트 자체가 없음** (시설 출입 기록 미생성 계열). 트래커 무결 | 없음 (트래커 정상). 시설 운영 문의 안내 |
| 2026-07-22 | [Jail] "입실 중인데 재실에 여러 명(본인 포함) 안 뜸" | 13:36 스냅샷엔 실제로 부재 → ①출입기록 서버→API 적재 지연(13:36 기준 당일 기록 19명 → 15:18엔 26명) ②GitHub 스케줄러 정전(~13:00-15:15 전 run 0회, 4스트림도 정지)으로 대시보드가 100분 구본에 정지. 수동 디스패치 직후 지목 3명 전부 재실 표시 | 원인 1의 복합형 — 소스 적재 지연 × 스케줄러 정전 | 수동 수집으로 해소. 스케줄러 정전 재발(7/18 이어 두 번째) |
| 2026-07-22 | 코드스페이스 로그인서버 Secret 동기화 401 | login-server가 쓰는 GH_PAT_SYNC 토큰 무효 (만료/폐기 추정) | 코드스페이스 로그인 경로로 세션 동기화 불가 — 허브 자동로그인(4시간 주기)만 남은 상태 | Codespaces 시크릿 GH_PAT_SYNC를 유효 PAT으로 교체 (사용자 작업) |
| 2026-07-22 | (참고) 수동 프로브가 secom/seat 단건을 전원 404로 반환 | bare JSESSIONID 직접호출의 한계 — 수집기 경로로는 정상 조회됨. 프로브는 세션 쿠키 구성이 수집기와 달라 미인증 404 | 프로브 도구 한계 | 판정은 항상 수집기 산출물(아티팩트) 기준으로 |
| 2026-07-24 | [전 트래커] 허브/EV/Quest 수집 전면 실패("길드 멤버 0명"), Jail은 0명 data.json 배포 | 스케줄러 정상·세션 정상·API 구조 동일 확인. 프로브 매트릭스: 어제 149명 나오던 (5,9) 포함 전 시즌×주차 조합이 `members:[]`·`myInfo:null`. 신규 번들 흐름 추적으로 `guild/season/currentSeasonDetail` 실측 → **시즌 1(ID 5) 7/24 종료 → 시즌 2(ID 6) 7/25~8/21 시작 사이의 길드 편성 공백기**. 사이트 UI에 "길드가 보인" 건 길드 카드/이력이라 편성과 무관 | **원인 7 — 소스 측 계절성 데이터 리셋** + Jail의 빈 갱신 미차단 결함 | ①모든 수집기에 "갱신 0명 → 마지막 알려진 명부 폴 백" 적용 (commons 허브/EV/Quest/Jail 4곳, 당일 push 후 수동 디스패치로 전원 치유 실검증) ②허브는 0명 시 기존 roster 유지+정상 종료로 변경 ③신규 번들의 시즌/주차 자동 발견 흐름 문서화 → 자동 발견 통합 (제안) |
| 2026-07-25 | [EV] 홈페이지 배포 후 대시보드에 구·신 과제명 혼재 | 소스가 7/24 과제 13종 명칭 전면 개명 (getUqstnlist 행의 evlGuide.mdfcnDt 7/24 15:21~23 KST + searchList 라이브 신명칭 반환 실측). Quest는 7/25 08:06 run이 개명 이후라 자동 갱신됐으나, EV는 병합 수집 설계상 과거 이벤트 명칭이 자동 갱신되지 않아 5~7월 1,074건 중 1,056건이 구명칭 잔존 | 소스 측 **과제 마스터 개명** + 병합 수집의 "과거 이벤트는 명칭 유지" 설계 (원인 분류 外 — 소스 데이터 개명 계열) | `refresh_names.js` 추가: 수강생별 searchList를 재조회해 (evlNo,evlDegr) 실측 매칭으로 명칭 교정. **999건 라이브 매칭 교정** (새 1:1 개명 테이블 13쌍 확정, 충돌 0) + 목록 윈도우 밖 **75건은 확정 테이블로 보정** (5월 40/6월 34/7월 1). Quest 조치 불필요 |

## 2. 구조적 원인 분류

1. **수집 지연** — 30분 주기 + GitHub 스케줄러 지연. 방금 생긴 평가는 최장 1~2시간 뒤에야 보일 수 있다(4스트림으로 완화했지만 0은 아님). 또 상위 시스템(출입기록 서버→API)의 적재 지연이 겹치면 같은 시각에도 멤버별 보유 기록이 어긋난다 (7/22 실측: 13:36 당일 19명 → 15:18 26명). 스케줄러 자체가 정전되면 주기조차 무의미해진다.
2. **표시 계층의 선택적 노출** — 캘린더 셀에는 완료(✔)/취소(✖)만 표시되고 진행/요청은 날짜 클릭 모달 뒤에 있었다. CANCELLED는 정책상 화면 제외. 겉으로는 "없음"과 구분 불가.
3. **요청단계(평가자 미상) 행의 시각 의미** — `s-` 접두 합성 행은 슬롯 시각이 아니라 피평가자 과제가 진행 상태로 바뀐 시각(`evlBgngDt`)을 쓴다. 실제 평가 슬롯은 항상 :00/:30이라, 17:39 같은 시각은 "가짜 평가"처럼 보여 혼란 유발.
4. **소스 시스템의 상태 지연** — 세션(슬롯)이 끝나도 소스가 완료로 전환하기 전까지 API는 "진행"을 돌려준다. 트래커는 미러이므로 그 상태를 그대로 노출한다 (예: 21:30~22:00 세션이 22:25 수집에도 진행).
5. **클라이언트 참조 시차** — 자동갱신(5분)은 탭이 보일 때만 동작한다. 오래 열어 둔 탭은 구버전 JSON을 보고 있을 수 있다.
6. **소스에 이벤트 자체가 없음** — 시설 출입 태그 미인식·계정 매핑 이상처럼 상위 시스템이 사건을 만들지 않은 경우. 트래커는 미러할 것이 없다. 판정법: 수집기 원본 아티팩트에서 해당 멤버 항목 직접 확인 + 타 멤버 동시간 데이터로 수집기 건재 입증.
7. **소스 측 계절성 데이터 리셋** — 길드 시즌 종료~신시즌 편성 사이처럼 소스의 정규 주기 전환으로 조회 결과가 통째로 비는 경우. HTTP/code는 정상이라 실패로 보이지 않는다 (7/24: (5,9)를 포함한 전 조합 `members:[]`, `myInfo:null` — 편성 공백기). 판정법: 여러 시즌/주차 조합 매트릭스 + `currentSeasonDetail`·`weeksList`로 현재 유효값 실측. 대응: 갱신 결과 0명은 실패로 간주해 **마지막 알려진 데이터로 폴 백** (빈 데이터 배포 금지).

## 3. 신고 접수 시 삼각측량 절차 (5단계)

1. **최신본 확인** — 라이브 `data/YYYY-MM.json`의 `meta.generatedAt` (7/20 22:25 신고 대상본은 정상 최신이었다).
2. **수집 여부 판정** — 해당 mbrId+과목으로 JSON 이벤트 검색. 없으면 수집 문제(세션 만료·스케줄러·API 변동), 있으면 표시 문제.
3. **첫 진입 시각** — `git log -S "<evalId>" --reverse -- docs/data/YYYY-MM.json`으로 데이터 진입 커밋 확인 → "신고 시점에 진짜 없었나" 판정.
4. **상태 매핑 확인** — 소스가 진행/완료 중 뭘 반환하는지 (완료 대기 상태인 건을 완료로 바꿔 칠하지 않는다).
5. **그래도 다르면** — 브라우저 강력 새로고침, 월 네비게이션, 히트맵 필터 칩 점검.

## 4. 해결책 현황

- [x] 4스트림 크론 — 스케줄러 누락 완화 (07-18)
- [x] CANCELLED 화면 제외 + 데이터 보존 (07-18)
- [x] 동시 run 커밋 충돌 재시도 로직 (07-18)
- [x] 캘린더 셀에 진행 ◔ / 요청 ▷ 카운트 + 헤더 범례 (07-20) → **▷ 요청 카운트·범례는 08-01 제거** (표시 정책 변경: 요청/예약 미노출)
- [x] 종료 시각이 지난 IN_PROGRESS 건 "진행(확정 대기)" 표기 (07-22 배포, 실검증 완료)
- [x] `s-` 합성 행 시각을 "≈" + 툴팁으로 구분, 상세 모달 "요청 단계 기준" 라벨 (07-22 배포) → **08-01 제거** (요청 단계 행 자체를 표시하지 않게 되어 실효 상실)
- [x] 외부 워치독용 `repository_dispatch(external-collect)` 트리거 4레포 심기 + Cloudflare Worker 스크립트 (07-22, codyssey_watchdog 레포 — 실배포는 사용자)
- [x] 갱신 결과 0명 시 마지막 알려진 명부 폴 백 — commons 허브/EV/Quest/Jail 4곳 (07-24 사태 당일 적용, 수동 디스패치 실검증)
- [x] 허브 0명 시 기존 roster 유지 + 정상 종료 (알림 폭탄 방지) (07-24)
- [x] 길드 시즌/주차 **자동 발견** (`currentSeasonDetail`→`weeksList`) commons 수집기 통합 — 하드코딩 노브 폐지 (07-24, 허브 vars 해제 + 라이브 `자동 발견: 6/1` 실검증)
- [x] 과거 평가 이벤트 과제명 라이브 교정기 `refresh_names.js` + 5/6/7월 1,074건 교정 (07-25: 999건 라이브 매칭 + 75건 확정 테이블 보정, 리네임 재발 시 재사용 가능)
- [ ] (제안) 상태줄에 다음 수집 예정 시각 또는 최근 수집 run 결과 배지

---

*새 사고가 발생하면 위 연표에 행을 추가하고, 원인이 5분류 밖이면 분류를 늘린다.*
